pragma Singleton

import QtQuick 2.13

QtObject {
    function openNotebook() {
        //EntryModel.open()
    }
    function appendEntry(entry) {
        //print(EntryModel.setData())
        //EntryModel.append(entry)
    }
}
